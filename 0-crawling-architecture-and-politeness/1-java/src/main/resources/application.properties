server.port=${PORT:3000}
spring.datasource.url=jdbc:postgresql://${PG_HOST:localhost}:${PG_PORT:5432}/${PG_DB:crawler}
spring.datasource.username=${PG_USER:crawler}
spring.datasource.password=${PG_PASSWORD:crawler}
spring.jpa.hibernate.ddl-auto=update
spring.data.redis.url=${REDIS_URL:redis://localhost:6379}
